<Window x:Class="Gu.Wpf.AutoRowGrid.Demo.AutoRowsAndColumnsExplicitHeightWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:autoRowGrid="http://gu.se/AutoRowGrid"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:demo="clr-namespace:Gu.Wpf.AutoRowGrid.Demo"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        Title="AutoRowsAndColumnsExplicitHeightWindow"
        SizeToContent="WidthAndHeight"
        mc:Ignorable="d">

    <Window.DataContext>
        <demo:ViewModel />
    </Window.DataContext>

    <GroupBox Width="150"
              Style="{StaticResource UiTestGroupBox}">
        <autoRowGrid:Grid ColumnDefinitions="Auto *" RowHeight="100">
            <autoRowGrid:Row>
                <TextBox x:Name="R0C0" Text="foo1" Style="{StaticResource LabelTextBox}"/>
                <TextBox x:Name="R0C1" Text="{Binding Value1}" />
            </autoRowGrid:Row>

            <autoRowGrid:Row>
                <TextBox x:Name="R1C0" Text="foo2"  Style="{StaticResource LabelTextBox}"/>
                <TextBox x:Name="R1C1" Text="{Binding Value2}" />
            </autoRowGrid:Row>
        </autoRowGrid:Grid>
    </GroupBox>
</Window>
